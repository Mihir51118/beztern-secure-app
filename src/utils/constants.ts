// API URL - would be set to the actual backend URL in production
export const API_URL = 'http://localhost:3000/api';

// Encryption key - in a real application, this would be stored securely
// and not hardcoded in the frontend
export const ENCRYPTION_KEY = 'beztern-secure-key-2025';